[package]
name = "autoagents-llamacpp-vulkan-py"
version.workspace = true
edition.workspace = true
description = "Python bindings for AutoAgents llama.cpp Vulkan backend"
license.workspace = true
publish = false

[lib]
name = "_autoagents_llamacpp_vulkan"
crate-type = ["cdylib"]

[features]
default = []
extension-module = ["pyo3/extension-module"]
vulkan = ["autoagents-llamacpp-py-core/vulkan"]

[dependencies]
autoagents-llamacpp-py-core = { path = "../autoagents-llamacpp-core" }
pyo3 = { version = "0.28.2", features = ["abi3-py39"] }
